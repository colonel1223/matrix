# Matrix Site

Live site:
- https://colonel1223.github.io/matrix/

## Quick update (2 commands)

1) Edit `index.html`
2) Deploy:

```bash
cd ~/matrix-site
git add -A && git commit -m "update" && git push


Now commit + push README:

```bash
git add README.md
git commit -m "Add README update instructions" || true
git push

cd ~/matrix-site && git add -A && git commit -m "update" && git push

